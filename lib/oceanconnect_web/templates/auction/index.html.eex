<div class="content has-margin-top-lg">
<h1 class="title is-3">Auction Listing</h1>
<table class="table is-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th>Vessel</th>
      <th>Port</th>
      <th>Company</th>
      <th>PO</th>
      <th>ETA</th>
      <th>ETD</th>
      <th>Auction Start</th>
      <th>Duration</th>
      <th>Anonymous Bidding</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for auction <- @auctions do %>
    <tr>
      <td><%= auction.id %></td>
      <td><%= auction.vessel %></td>
      <td><%= auction.port %></td>
      <td><%= auction.company %></td>
      <td><%= auction.po %></td>
      <td><%= format_datetime(auction.eta) %></td>
      <td><%= format_datetime(auction.etd) %></td>
      <td><%= format_datetime(auction.auction_start) %></td>
      <td><%= auction.duration %></td>
      <td><%= auction.anonymous_bidding %></td>

      <td class="text-right">
        <%= link "Show", to: auction_path(@conn, :show, auction), class: "button is-primary is-small" %>
        <%= link "Edit", to: auction_path(@conn, :edit, auction), class: "button is-primary is-small" %>
        <%= link "Delete", to: auction_path(@conn, :delete, auction), method: :delete, data: [confirm: "Are you sure?"], class: "button is-danger is-small" %>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<%= link "New Auction", to: auction_path(@conn, :new), class: "button" %>
</div>
