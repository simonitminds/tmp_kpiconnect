<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Oceanconnect!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
<%# TODO: Bring Font Awesome 5 into assets so we don't have to use the cdn %>
    <link href="https://use.fontawesome.com/releases/v5.0.4/css/all.css" rel="stylesheet">
  </head>

  <body>
    <header class="header">
      <nav role="navigation" class="navbar is-primary is-fixed-top">
        <div class="navbar-brand">
          <a href="/auctions" class="navbar-item">
            <img src="<%= static_path(@conn, "/images/ocm_transparentlogo_reverse.png") %>">
          </a>
        </div>
        <%# TODO: Add in actual mobile implementation of nav... %>
        <!-- <button class="button navbar-burger">
          <span></span>
          <span></span>
          <span></span>
        </button> -->
        <div class="navbar-menu">
          <%# TODO: Add links as we add additional sections (when relevant) %>
          <div class="navbar-end">
            <span class="navbar-item has-text-weight-bold"><%= OceanconnectWeb.SessionView.current_user(@conn) %></span>
            <span class="navbar-item"><i class="is-size-6 is-block">(<%= @current_user.company.name %></i>)</span>
            <!-- <a href="#" class="navbar-item">Account Settings</a>
            <a href="#" class="navbar-item">Messages</a>
            <a href="#" class="navbar-item">Log Out</a> -->
            <%= OceanconnectWeb.SessionView.log_in_logout_link(@conn) %>
          </div>
        </div>
      </nav>
    </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>
      <script> window.userToken = "<%= assigns[:user_token] %>";</script>
      <script> window.userId = "<%= OceanconnectWeb.SessionView.current_user_id(@conn) %>";</script>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
